{
  "name": "signalworks-site",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:launch": "npm run build && ! grep -rlE 'PLACEHOLDER|signalworks\\.example' dist/ && echo 'LAUNCH CHECK PASSED: no placeholders in dist'"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/sitemap": "^3.0.0"
  }
}