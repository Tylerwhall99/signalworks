---
import { FREE_BUILD, NAV_CTA } from '../config';

interface Props {
  heading?: string;
  body?: string;
}

const {
  heading = 'Start your free site.',
  body = `Tell us about your business. Draft one lands within ${FREE_BUILD.draftTimeline}, and you own it either way.`,
} = Astro.props;
---

<section class="cta-band">
  <div class="container cta-band__inner">
    <div>
      <h2>{heading}</h2>
      <p>{body}</p>
    </div>
    <a href={NAV_CTA.href} class="btn btn--primary btn--large">{NAV_CTA.label}</a>
  </div>
</section>

<style>
  .cta-band {
    border-top: 1px solid var(--line);
    background: var(--signal-tint);
    padding-block: var(--space-6);
  }

  .cta-band__inner {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
  }

  .cta-band h2 {
    font-size: var(--text-2xl);
  }

  .cta-band p {
    margin-top: var(--space-2);
    color: var(--ink-soft);
    max-width: 48ch;
  }
</style>
